# LinkedIn Auto Apply Bot - Environment Variables
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!

# LinkedIn Login Credentials (Optional)
# If not set here, you can still set them in config/secrets.py
LINKEDIN_USERNAME=username@example.com
LINKEDIN_PASSWORD=example_password

# AI Configuration
# Set to true or false (case-insensitive)
USE_AI=True
AI_PROVIDER=openai
LLM_API_URL=https://api.openai.com/v1/
LLM_API_KEY=your-api-key-here
LLM_MODEL=gpt-3.5-turbo
LLM_SPEC=openai
STREAM_OUTPUT=False

#Tcl/Tk Configuration (Only edit if needed
TCL_LIBRARY=
TK_LIBRARY=

# Notes:
# - For OpenAI: Use https://api.openai.com/v1/ as LLM_API_URL
# - For local LLM (Ollama, LM Studio, etc.): Use http://127.0.0.1:1234/v1/ as LLM_API_URL and set LLM_API_KEY=not-needed
# - For DeepSeek: Use https://api.deepseek.com/v1/ as LLM_API_URL
# - For Gemini: Set AI_PROVIDER=gemini (LLM_API_URL may not be needed)

# Debug Mode
# Set to False to skip all confirmation dialogs for automated runs
DEBUG_MODE=True
